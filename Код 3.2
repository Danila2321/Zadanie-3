n = int(input())
arr = list(map(int, input().split()))
result = [-1] * n
stack = []

for i in range(n):
    while stack and arr[i] < arr[stack[-1]]:
        pos = stack.pop()
        result[pos] = arr[i]
    stack.append(i)

print(' '.join(map(str, result)))
