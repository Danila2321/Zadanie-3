s = input().strip()

balance = 0  
remove = 0  

for char in s:
    if char == '(':
        balance += 1
    else:
        if balance > 0:
            balance -= 1
        else:
            remove += 1

remove += balance
print(f"Скобки: '{s}'")
print(f"Кол-во удалений: {remove}")
